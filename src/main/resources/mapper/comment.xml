<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.dao.CommentDao">
<!--    <select id="getComment" resultType="com.community.model.CommentModel">-->
<!--        SELECT-->
<!--            c_id, b_id, c_content, userId-->
<!--        FROM-->
<!--            community_comment-->
<!--        WHERE-->
<!--            b_id = ${b_id}-->
<!--    </select>-->

    <insert id="insert" parameterType="String">
        INSERT INTO
            community_comment(b_id, recomment_id, c_content, c_date, userId)
        VALUES
            (${b_id}, (SELECT AUTO_INCREMENT
                        FROM information_schema.tables
                        WHERE table_name = 'community_comment'
                        AND table_schema = DATABASE()),
            #{c_content}, now(), #{userId})
    </insert>

    <insert id="secondInsert" parameterType="String">
        INSERT INTO
            community_comment(b_id, recomment_id, c_content, c_date, userId)
        VALUES
            (${b_id}, ${recomment_id}, #{c_content}, now(), #{userId})
    </insert>

    <update id="update" parameterType="String">
        UPDATE
            community_comment
        SET
            c_content = #{c_content}, updateCheck = 1
        WHERE
            c_id = ${c_id}
    </update>

    <delete id="delete" parameterType="String">
        DELETE FROM
            community_comment
        WHERE
            c_id = ${c_id}
    </delete>
</mapper>