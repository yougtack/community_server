<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.community.dao.BoardDao">
    <select id="getBoardList" resultType="com.community.model.BoardModel">
        SELECT
            b_id, b_type, b_title, b_date, userId
        FROM
            community
    </select>

    <insert id="insert" parameterType="String">
        INSERT INTO
            community(b_type, b_title, b_date, b_content, userId)
        VALUES
            (#{b_type}, #{b_title}, now(), #{b_content}, #{user_id})
    </insert>

    <update id="update" parameterType="String">
        UPDATE
            community
        SET
            b_type = #{b_type}, b_title = #{b_title}, b_content = #{b_content}, b_date = now()
        WHERE
            b_id = ${b_id}
    </update>

    <delete id="delete" parameterType="String">
        DELETE FROM
            community
        WHERE
            b_id = ${b_id}
    </delete>

    <select id="getView" resultType="com.community.model.ViewModel">
        SELECT
            b_id, b_type, b_title, b_content, b_date, userId
        FROM
            community
        WHERE
            b_id = ${b_id}
    </select>

</mapper>